# Compose phiên bản gọn (single-node, không Hadoop) để demo Airflow + Spark + Mongo.
# Data nằm trên local volume project/data, các container đều mount cùng path.
# Đầy đủ cho việc trigger DAG recsys_pipeline_v1 từ Airflow UI.
#
# Usage:
#   docker compose -f bigdata/docker-compose.yml up -d --build
#   open http://localhost:8082   (Airflow, admin/admin)
#   open http://localhost:8080   (Spark Master UI)
#
# Bắt buộc trước khi chạy: data/raw/{transactions_train,articles,customers}.csv
# (đã sinh sẵn bởi sample_dataset.py)

services:
  postgres:
    image: postgres:13
    environment:
      POSTGRES_USER: airflow
      POSTGRES_PASSWORD: airflow
      POSTGRES_DB: airflow
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD", "pg_isready", "-U", "airflow"]
      interval: 5s
      retries: 10
    networks: [bigdata]

  mongodb:
    image: mongo:6.0
    hostname: mongodb
    environment:
      MONGO_INITDB_DATABASE: hm_recsys
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db
    networks: [bigdata]

  spark-master:
    image: bitnamilegacy/spark:3.5
    hostname: spark-master
    environment:
      SPARK_MODE: master
      SPARK_RPC_AUTHENTICATION_ENABLED: "no"
      SPARK_RPC_ENCRYPTION_ENABLED: "no"
      SPARK_LOCAL_STORAGE_ENCRYPTION_ENABLED: "no"
      SPARK_SSL_ENABLED: "no"
      SPARK_USER: spark
    ports:
      - "8080:8080"   # Master UI
      - "7077:7077"   # Master RPC
    volumes:
      - ./apps:/opt/spark-apps
      - ../data:/workspace/data
    networks: [bigdata]

  spark-worker:
    image: bitnamilegacy/spark:3.5
    depends_on: [spark-master]
    environment:
      SPARK_MODE: worker
      SPARK_MASTER_URL: spark://spark-master:7077
      SPARK_WORKER_MEMORY: 4G
      SPARK_WORKER_CORES: 4
      SPARK_RPC_AUTHENTICATION_ENABLED: "no"
      SPARK_RPC_ENCRYPTION_ENABLED: "no"
      SPARK_LOCAL_STORAGE_ENCRYPTION_ENABLED: "no"
      SPARK_SSL_ENABLED: "no"
      SPARK_USER: spark
    ports:
      - "8081:8081"   # Worker UI
    volumes:
      - ./apps:/opt/spark-apps
      - ../data:/workspace/data
    networks: [bigdata]

  airflow:
    build:
      context: .
      dockerfile: docker-file.airflow.demo
    image: airflow-recsys:demo
    depends_on:
      postgres:
        condition: service_healthy
      spark-master:
        condition: service_started
      mongodb:
        condition: service_started
    environment:
      AIRFLOW__CORE__EXECUTOR: LocalExecutor
      AIRFLOW__DATABASE__SQL_ALCHEMY_CONN: postgresql+psycopg2://airflow:airflow@postgres:5432/airflow
      AIRFLOW__CORE__LOAD_EXAMPLES: "False"
      AIRFLOW__API__AUTH_BACKENDS: airflow.api.auth.backend.session,airflow.api.auth.backend.basic_auth
      AIRFLOW_CONN_SPARK_DEFAULT: '{"conn_type":"spark","host":"spark://spark-master","port":7077}'
      MONGO_URI: mongodb://mongodb:27017
      MONGO_DB: hm_recsys
      HDFS_BASE: file:///workspace
      PYTHONPATH: /opt/spark-apps
    ports:
      - "8082:8080"
    volumes:
      - ./dags:/opt/airflow/dags
      - ./apps:/opt/spark-apps
      - ./logs:/opt/airflow/logs
      - ../data:/workspace/data
    command: bash -c "
        airflow db migrate &&
        (airflow users create --username admin --password admin --firstname admin --lastname admin --role Admin --email admin@example.com || true) &&
        (airflow scheduler &) &&
        exec airflow webserver
      "
    networks: [bigdata]

networks:
  bigdata:
    driver: bridge

volumes:
  postgres-data:
  mongo-data:
